import { LikeSocket } from 'noice-json-rpc';
export declare class WebSocketToLikeSocketProxy {
    private _port;
    private _socket;
    private _server;
    private _currentlyOpenedWebSocket;
    constructor(_port: number, _socket: LikeSocket);
    start(): void;
}
