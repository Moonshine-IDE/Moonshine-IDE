package actionScripts.events
{
    import flash.events.Event;

    public class HiddenFilesEvent extends Event
    {

        public function HiddenFilesEvent(type:String)
        {
            super(type);
        }
    }
}
